// Crie um pequeno aplicativo Node.js que:

// 1. **Manipule arquivos:**
//     - Crie um arquivo `dados.txt` e escreva nele uma lista de 5 itens (um por linha).
//     - Leia o conteúdo de `dados.txt`.
//     - Adicione mais 2 itens ao final do `dados.txt`.
//     - Leia o conteúdo atualizado e imprima no console.
//     - Remova o arquivo `dados.txt`.
//     - Use `try-catch` para lidar com possíveis erros de leitura/escrita.
// 2. **Crie um servidor HTTP simples:**
//     - Utilize o módulo `http` do Node.js.
//     - O servidor deve responder na porta 3000.
//     - Quando acessado na rota `/`, deve retornar “Bem-vindo à minha API!”.
//     - Quando acessado na rota `/info`, deve retornar um JSON com informações básicas (ex: `{ "versao": "1.0", "autor": "Seu Nome" }`).
//     - Implemente um tratamento de erro básico para rotas não encontradas (retornar status 404 e mensagem “Página não encontrada”).

// **Instruções:**
// * Crie um novo arquivo `.js` para cada exercício ou um único arquivo com seções bem definidas.
// * Comente seu código explicando cada parte.
// * Teste seus códigos usando `node nome_do_arquivo.js` no terminal.
// * Para o Exercício 7, lembre-se de parar o servidor HTTP (Ctrl+C no terminal) após testá-lo.