// 1. Crie um array de objetos, onde cada objeto representa um livro com as propriedades `titulo`, `autor` e `anoPublicacao`.
// 2. Adicione um novo livro ao array.
// 3. Remova um livro específico do array (pelo título, por exemplo).
// 4. Use `map` para criar um novo array contendo apenas os títulos dos livros.
// 5. Use `filter` para criar um novo array contendo apenas os livros publicados após o ano 2000.
// 6. Use `reduce` para encontrar o livro mais antigo no array.
// 7. Crie um objeto `empresa` com propriedades como `nome`, `cnpj`, e um array de `funcionarios`. Cada funcionário deve ser um objeto com `nome`, `cargo` e `salario`.
// 8. Adicione um novo funcionário à empresa.
// 9. Calcule o salário total de todos os funcionários da empresa.
