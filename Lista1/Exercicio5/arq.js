// 1. Crie uma função chamada `calcularMedia` que recebe um array de números como argumento e retorna a média desses números.


// 2. Crie uma arrow function chamada `ehPar` que recebe um número e retorna `true` se for par e `false` caso contrário.


// 3. Crie uma função `gerarSaudacao` que recebe `nome` e `sobrenome` como argumentos. Se `sobrenome` não for fornecido, ele deve ter um valor padrão de “Desconhecido”. A função deve retornar uma saudação completa.


// 4. Crie uma função de ordem superior (higher-order function) chamada `operacaoMatematica` que recebe uma função (`soma`, `subtracao`, `multiplicacao`, `divisao`) e dois números. Ela deve executar a função passada com os números e retornar o resultado.


